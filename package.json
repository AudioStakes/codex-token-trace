{
  "name": "codex-token-trace",
  "version": "0.3.0",
  "description": "Trace what drives Codex token usage from local session logs.",
  "type": "module",
  "private": true,
  "bin": {
    "codex-token-trace": "./dist/cli.js",
    "ctt": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "fix": "npm run format && npm run lint:fix",
    "verify": "npm run format:check && npm run lint && npm run test && npm run build && npm run dev -- --help && npm run dev -- analyze --json tests/fixtures/minimal-session.jsonl && npm run dev -- diagnose --limit 2 tests/fixtures/diagnose-session.jsonl",
    "check": "npm run verify"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
