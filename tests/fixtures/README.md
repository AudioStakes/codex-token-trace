# Test fixtures

Keep fixtures small and artificial. Do not commit real `~/.codex/sessions` logs.

Real Codex logs may contain prompts, code, command output, file paths, image/tool metadata, and other sensitive data.
